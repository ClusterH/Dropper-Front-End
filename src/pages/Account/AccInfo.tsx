import React from 'react'
import { MainButton } from '../../components/Buttons/MainButton'
import { ComponentWrapper, ContainerColumn } from '../../styles/globalStyles'

export const AccountInfo: React.FC = () => {
  return <ContainerColumn width={'20%'} backgroundColor={'var(--secondary)'}></ContainerColumn>
}
