import React from 'react'
import { ContainerColumn } from '../../styles/globalStyles'

export const AccountInfo: React.FC = () => {
  return <ContainerColumn width={'20%'} backgroundColor={'transparent'}></ContainerColumn>
}
